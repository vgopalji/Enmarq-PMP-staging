@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model CareStream.Models.ProductFamilyModel

@{
    ViewData["Title"] = "CreateProductFamily";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div id="createProductFamily">

    <div class="container no-gutter border-bottom mb-4">
        <div class="row">
            <nav class="navbar navbar-expand-lg navbar-light pt-0 pb-0">
                <a class="navbar-brand" href="#"></a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="nav-tabs navbar-nav">
                        <li class="nav-item active col-12 mt-2 mb-2">
                            <span class="font-weight-bold"> Create a New Product Family </span>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
    </div>

    <div class="container">
        <div class="row ">
            <form method="post" action="/ProductFamily/Post">

                <div class="form-group row mb-3">
                    <label for="staticname" class="col-sm-2 col-form-label">
                        Product Family Name
                        @*<span class="text-danger">*</span>*@
                    </label>
                    <div class="col-sm-6">
                        <input type="text" class="form-control" id="productFamilyName" placeholder="Enter Product Family Name" asp-for="@Model.ProductFamilyName" required>
                        <span asp-validation-for="@Model.ProductFamilyName" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group row mb-3">
                    <label for="inputDescription" class="col-sm-2 col-form-label">Description </label>
                    <div class="col-sm-6">
                        <textarea class="form-control" id="productFamilyDescription" rows="3" placeholder="Enter Description" asp-for="@Model.ProductDescription"></textarea>
                        <span asp-validation-for="@Model.ProductDescription" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group row mb-3">
                    <label for="inputDescription" class="col-sm-2 col-form-label">Dealers </label>
                    <div class="col-sm-6">
                        <a href="#" class="navbar" data-target="#bs-canvas-right" data-toggle="canvas"
                           aria-expanded="false" aria-controls="bs-canvas-right"
                           id="dealerProductFamily">
                            <label id="dealerLab" style="cursor:pointer"> No Dealers Selected</label>
                        </a>
                    </div>
                </div>
                <div class="form-group row mb-3">
                    <label for="inputDescription" class="col-sm-2 col-form-label">Products </label>
                    <div class="col-sm-6">
                        <a href="#" class="navbar" data-target="#bs-canvas-left" data-toggle="canvas"
                           aria-expanded="false" aria-controls="bs-canvas-right"
                           id="productProductFamily">
                            <label id="memberLab"> Add New Products </label>
                        </a>
                    </div>
                </div>
                <div class="form-group row mb-3">
                    <div class="form-group row mb-3">
                        <div class="col-sm-8 text-right">
                            <input type="submit" class="btn btn-primary text-white" value="Create" onclick="blockUi();" />
                        </div>
                    </div>
                </div>
            </form>

        </div>
    </div>
</div>

<div id="dealerDiv">
    <div id="bs-canvas-right" class="bs-canvas bs-canvas-anim bs-canvas-right position-fixed h-100" style="padding-bottom:10px;">
        <partial name="_Dealers" model="@Model" />
        <a id="selectedDealer" style="cursor:pointer" class="bs-canvas-close btn btn-primary text-white ml-4" aria-label="Close">Select</a>
        <a id="selectedGroupOwnerClose" style="cursor:pointer" class="bs-canvas-close btn btn-primary text-white ml-4" aria-label="Close">Close</a>
    </div>
</div>

@*<div id="productDiv">
        <div id="bs-canvas-left" class="bs-canvas bs-canvas-anim bs-canvas-right position-fixed h-100" style="padding-bottom:10px;">
            <partial name="_UserGroup" model="@Model.GroupMemberAssign" />
            <a id="selectedGroupMember" style="cursor:pointer" class="bs-canvas-close btn btn-primary text-white ml-4" aria-label="Close">Select</a>
            <a id="selectedGroupMemberClose" style="cursor:pointer" class="bs-canvas-close btn btn-primary text-white ml-4" aria-label="Close">Close</a>
        </div>
    </div>*@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script type="text/javascript">

        $(document).ready(function () {

            $('#selectedDealer').on('click', function () {
                var selectedDealerIds = [];
                $("input[type='checkbox'][id^='chkbox_Dealer']:checked").each(function () { selectedDealerIds.push($(this).data("memberid")); })

                if (selectedDealerIds.length == 0) {
                    $('#dealerLab').html('<span style="text-transform:capitalize;">' +  'No Dealers Selected </span>');
                }
                else if (selectedDealerIds.length > 0) {
                    var selDealer = ' Dealers Selected';
                    if (selectedDealerIds.length == 1) {
                        selDealer = ' Dealer Selected'
                    }
                    $('#dealerLab').html('<span style="text-transform:capitalize;">' + selectedDealerIds.length + selDealer + '</span>');
                }

                
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("AddSelectedDealers", "ProductFamily")',
                    contentType: 'application/json; charset=utf-8',
                    dataType: "json",
                    cache: false,
                    beforeSend: function (xhr) {
                        xhr.setRequestHeader("XSRF-TOKEN",
                            $('input:hidden[name="__RequestVerificationToken"]').val());
                    },
                    data: JSON.stringify({
                        SelectedDealers: selectedDealerIds
                    }),
                    success: function (response) {
                    },
                    failure: function (response) {
                        alert(response);
                    }
                })
            })

            $('#selectedGroupMember').on('click', function () {
                var selectedMemberIds = [];
                $("input[type='checkbox'][id^='chkbox_Member']:checked").each(function () { selectedMemberIds.push($(this).data("memberid")); })


                if (selectedMemberIds.length == 0) {
                    $('#memberLab').html('<span style="text-transform:capitalize;">' + 'No Members Selected </span>');
                }
                else if (selectedMemberIds.length > 0) {
                    var selOwner = ' Members Selected';
                    if (selectedMemberIds.length == 1) {
                        selOwner = ' Member Selected'
                    }
                    $('#memberLab').html('<span style="text-transform:capitalize;">' + selectedMemberIds.length + selOwner + '</span>');
                }

                var groupId = "Member"
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("AddSelectedOwnerOrMember", "Group")',
                    contentType: 'application/json; charset=utf-8',
                    dataType: "json",
                    cache: false,
                    beforeSend: function (xhr) {
                        xhr.setRequestHeader("XSRF-TOKEN",
                            $('input:hidden[name="__RequestVerificationToken"]').val());
                    },
                    data: JSON.stringify({
                        GroupId: groupId,
                        SelectedMembers: selectedMemberIds
                    }),
                    success: function (response) {
                    },
                    failure: function (response) {
                        alert(response);
                    }
                })
            })

            $("#dealerProductFamily").click(function (e) {
                e.preventDefault();
                $('#dealerDiv').show();
                //$('#productDiv').hide();

            });

            $("#productProductFamily").click(function (e) {
                e.preventDefault();
                //$('#productDiv').show();
                $('#dealerDiv').hide();
            });

            $('#OffsetWizDiv').DataTable({
                'columnDefs': [{
                    'targets': 0,
                    'searchable': false,
                    'orderable': false,
                    'className': 'dt-body-center'

                }],
                select: {
                    style: 'os',
                    selector: 'td:first-child'
                },
                order: [[1, 'asc']],
                "language": {
                    "search": "",
                    "searchPlaceholder": "Search Dealers",
                },
                "bLengthChange": false,
                "bPaginate": false,
                "bInfo": false,
                "fixedHeader": {
                    "footer": false
                },
            });

            var bsDefaults = {
                offset: false,
                overlay: true,
                width: '372px'
            },
                bsMain = $('.bs-offset-main'),
                bsOverlay = $('.bs-canvas-overlay');

            $('[data-toggle="canvas"][aria-expanded="false"]').on('click', function () {
                var canvas = $(this).data('target'),
                    opts = $.extend({}, bsDefaults, $(canvas).data()),
                    prop = $(canvas).hasClass('bs-canvas-right') ? 'margin-right' : 'margin-left';

                if (opts.width === '100%')
                    opts.offset = false;

                $(canvas).css('width', opts.width);
                if (opts.offset && bsMain.length)
                    bsMain.css(prop, opts.width);

                $(canvas + ' .bs-canvas-close').attr('aria-expanded', "true");
                $('[data-toggle="canvas"][data-target="' + canvas + '"]').attr('aria-expanded', "true");
                if (opts.overlay && bsOverlay.length)
                    bsOverlay.addClass('show');
                return false;
            });

            $('.bs-canvas-close, .bs-canvas-overlay').on('click', function () {
                var canvas, aria;
                if ($(this).hasClass('bs-canvas-close')) {
                    canvas = $(this).closest('.bs-canvas');
                    aria = $(this).add($('[data-toggle="canvas"][data-target="#' + canvas.attr('id') + '"]'));
                    if (bsMain.length)
                        bsMain.css(($(canvas).hasClass('bs-canvas-right') ? 'margin-right' : 'margin-left'), '');
                } else {
                    canvas = $('.bs-canvas');
                    aria = $('.bs-canvas-close, [data-toggle="canvas"]');
                    if (bsMain.length)
                        bsMain.css({
                            'margin-left': '',
                            'margin-right': ''
                        });
                }
                canvas.css('width', '');
                aria.attr('aria-expanded', "false");
                if (bsOverlay.length)
                    bsOverlay.removeClass('show');
                return false;
            });

            unBlockUi();
        });

    </script>
}
